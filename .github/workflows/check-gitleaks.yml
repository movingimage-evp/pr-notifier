# ==============================================================================
# DO NOT EDIT - THIS FILE IS MANAGED EXTERNALLY
#
# It is deployed and kept in sync by Terraform from:
#   https://github.com/movingimage-evp/mi-github-configurations
#
# Any change made to this file in this repository will be silently overwritten
# on the next Terraform apply. This applies to humans and to AI coding agents
# alike: do not edit, rename, move or delete this file here.
#
# To change it, open a pull request against mi-github-configurations
# (maintainer: Team300 / admin@movingimage.com).
# ==============================================================================
name: gitleaks
on: [pull_request, push, workflow_dispatch]
jobs:
  scan:
    name: gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE}}
          GITLEAKS_ENABLE_UPLOAD_ARTIFACT: false
          GITLEAKS_NOTIFY_USER_LIST: "movingimage-evp/chapter_devops"